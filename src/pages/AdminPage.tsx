// FILE: src/pages/AdminPage.tsx (Versione Semplificata)

import React from 'react';

// Stile per centrare il contenuto nella pagina
const pageStyle: React.CSSProperties = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100vh',
  width: '100%',
  fontSize: '2.5rem',
  fontWeight: 'bold',
  color: '#cccccc',
  backgroundColor: '#1a1a1a'
};

export default function AdminPage() {
  return (
    <div style={pageStyle}>
      adminPage
    </div>
  );
}